package com.domainsgraph.data;

import com.google.inject.Binder;
import com.google.inject.Module;

public class DomainsGraphDataModule implements Module {
  @Override
  public void configure(Binder binder) {

  }
}
